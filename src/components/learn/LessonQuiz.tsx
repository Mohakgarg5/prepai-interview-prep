'use client'
export function LessonQuiz({ lessonTitle, practiceQuestions, isComplete, onComplete }: {
  lessonTitle: string
  practiceQuestions: string[]
  isComplete: boolean
  onComplete: (score?: number) => void
}) {
  return null
}
